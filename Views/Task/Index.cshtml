@model IEnumerable<GestorTareas.Models.Tarea>

<h1>Tareas</h1>

<table class="table">
    <thead>
        <tr>
            <th>Nombre</th>
            <th>Descripción</th>
            <th>Acciones</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var tarea in Model)
        {
            <tr>
                <td>@tarea.Name</td>
                <td>@tarea.Description</td>
                <td>
                    <a asp-action="Edit" asp-route-id="@tarea.Id">Editar</a> |
                    <a asp-action="Delete" asp-route-id="@tarea.Id">Eliminar</a>
                </td>
            </tr>
        }
    </tbody>
</table>
